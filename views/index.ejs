<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dog Pics</title>
    <link rel="stylesheet" href="styles/bootstrap.css" />
    <link rel="stylesheet" href="styles/main.css" />
  </head>
  <body>
    <div class="container col-xxl-8 px-4 py-5">
      <div class="row flex-lg-row-reverse align-items-center g-5 py-5">
        <div class="col-10 col-sm-8 col-lg-6">
          <% if(locals.dogUrl) { %>
          <img src="<%= dogUrl %>" alt="" />
          <% } %>
        </div>
        <div class="col-lg-6">
          <h1 class="display-5 fw-bold lh-1 mb-3">Dog Images</h1>
          <p class="lead">
            Don't have a dog at home and your feeling bored? No worries you can
            play fetch with our random dogs.
          </p>
          <div class="d-grid gap-2 d-md-flex justify-content-md-start">
            <form action="/get-dog-pic" method="GET">
              <button type="submit" class="btn btn-primary btn-lg px-4 me-md-2">
                Play Fetch!
              </button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
